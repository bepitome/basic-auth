/* eslint-disable-next-line */
const APP = {
    NAME: "bauth",
    VERSION: "1.0.0",
    BUILD: "0.0.1",
};

const DATABASE = {
    NAME: "bauthdb",
    VERSION: "1.0.0",
};

const COLLECTION = {
    USERS: "users",
};

module.exports = { APP, DATABASE, COLLECTION };