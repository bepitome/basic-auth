/* eslint-disable-next-line */
const APP = {
    NAME: "test_app",
    VERSION: "1.0.0",
    BUILD: "0.0.1",
};

const DATABASE = {
    NAME: "test_database",
    VERSION: "1.0.0",
};

const COLLECTION = {
    ACCOUNTS: "test_accounts",
};

module.exports = { APP, DATABASE, COLLECTION };